/**
 * Neutral content chip — destination tags in the facilities directory.
 * @startingPoint section="Core" subtitle="Neutral content chip" viewport="700x150"
 */
export interface PillProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}

export function Pill(props: PillProps): JSX.Element;
